{
  "buildCommand": "prisma generate && node fix-prisma-imports.js && next build",
  "rewrites": [
    {
      "source": "/api/payments/webhook",
      "destination": "/api/payments/webhook/"
    }
  ],
  "crons": [
    {
      "path": "/api/cron/send-course-emails",
      "schedule": "0 9 * * *"
    }
  ]
}
